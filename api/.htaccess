RewriteEngine On

# Allow direct access to existing PHP files (e.g., health.php)
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Route all other requests to the front controller
RewriteRule ^(.*)$ index.php/$1 [QSA,L]
